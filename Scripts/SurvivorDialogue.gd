extends Control

@export_multiline var survivor1Array: PackedStringArray = [""]
@export_multiline var survivor2Array: PackedStringArray = [""]
@export_multiline var survivor3Array: PackedStringArray = [""]

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
